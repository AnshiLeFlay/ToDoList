import React from "react";
import { Layout } from "antd";

const Footer: React.FC = () => {
    const { Footer } = Layout;

    return (
        <Footer style={{ textAlign: "center" }}>Denis Pominov © 2023</Footer>
    );
};

export default Footer;
